{
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "framework": "parcel",
  "routes": [
    { "src": "/assets/(.*)", "dest": "/assets/$1" },
    { "src": "/(.+)", "dest": "/$1" }
  ]
}